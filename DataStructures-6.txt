A-1 --> C
A-2 ---> B
C-1 ---> B
A-3 ---> C
B-1 --->A
B-2 -->C
A-1 --> C

n --> disks

pow(2,n) -1
63--->

263 

def towers(n, source,destination,auxilary):
	if n==1
	print(f"move from {soure} to {destination}")
	else
	towers(n-1,source,auxillary, destionation)
	print("move disk {n} from {source} to {destination}")
	towers(n-1, {auxillary}, {destination}, {source})


towers(3,A,C,B)

towers(2,A,B,C)--> towers(1,A,B,C) ---> move from A to C

To find the shortest path from source to destination
----------------------------------------------------

A---traverse all other nodes ---> A

1. Delivery route optimization

2. Circuti Board drilling

3. DNA Sequencing

4. Ride Sharings



Job Sequencing Algorithms
--------------------------


5sec---->

j1(2)-->20, j2(2)-->60,j3(1), j4(3), j5(4)-----> 

1 to n-1
n=5
for i in range(n)
	for j in range (n-1-i)


Algorithm
----------

1. sort jobs by decreasing order of profit
2. for each joB schedule it to the latest available time slot before on its deadline

3. if the slot is already filled, skip the job


0/1 knapsack / fractional knapsack


Algorithm for fractinal kanpsack problem
----------------------------------------
1. consider all the items with their weights and profits mentioned 

2. calulate pi/wi of all the items in descending order based on their ratio

3. without exceeding the limit, add the items into the knapsack

4. if the knapsack can still store more weight, but the weights of the other
   items exceed the limit, the fractional part of the next time can be added


5. Hence giving it the name called franctional knapsack problem

0/1 Knapsack Problem
---------------------
Brute Force Apporach

1. calcuate the value of every possible comination of items in the knapsack
2. discard the combinations that are heavier than the knapsack weight
3. choose the combination of items with the highest value

1. Top Down Approach
2. Bottom Up approach
3. Tabulation
4. Recursive

Pattern Matching Algorithm
-------------------------
1. Naive alogorithm

given --> text with length n and a pattern with length m
the task is to print all occurances of pattern in the text

text = THIS IS A TEST TEXT
pattern = "TEXT" ---> index =10
pattern = "THIS" ---> index 0

text = 'AABAACAADAABAABA'
pattern = AABA --> index = 0

Time complexity ---> O(n2)
Best Case ----> O(n)

KMP pattern Searching Algorithm
-------------------------------
Knuth- Morris - Prett

txt

pattern


match

LPS Array

Proper suffix --> suffix is not equal to whole string itself.

Suffix  -- A substring that ends at the current position

Rabin -Karp Algorithm
---------------------
uses hashing to find the patterns in the text efficiently

--> useful when wants to search multiple patterns in a text

h(x) ---> y

h(ABC)--> y

65+66+67--> 198

h(BCA) --> 198

HELLO ABC and BCABC THIS A ABC PROGRAM

ABC

complexity -- Avg -- O(n+m)

Worst case - O(n*m)


